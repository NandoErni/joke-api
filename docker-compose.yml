services:
  joke-api:
    image: nandoerni/joke-api:latest
    ports:
      - "8080:8080"
    environment:
      - API_KEY=${API_KEY}
      - DB_PATH=${DB_PATH}
    volumes:
      - /srv/joke-api/data:/data
    restart: always